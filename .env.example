# Read by the server actions in app/actions.ts. Dashboard → Project settings →
# API keys. The project ref itself lives in trigger.config.ts.
TRIGGER_SECRET_KEY=tr_dev_xxxxxxxxxxxx

# Read by the local dev worker — `trigger dev` loads .env / .env.local /
# .env.development / .env.development.local. For deployed environments, set this
# in the dashboard under Environment Variables instead.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# Read by lib/db.ts (via @vercel/postgres) for the published-games gallery —
# see app/actions.ts and lib/published-games.ts. Add "Postgres" from your
# Vercel project's Storage tab (auto-injects this for deployed environments),
# then for local dev run `vercel env pull` or copy the value from the
# dashboard yourself. After it's set, run `npm run setup:db` once to create
# the tables.
POSTGRES_URL=postgres://xxxxxxxxxxxx
